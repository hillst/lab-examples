
rule split_sort_bams:
    input:
        bam="{prefix}/{bamname}.bam"
    output:
        "{prefix}/split_bams/{bamname}.{chromosome}.sorted"
    shell:
        "samtools view -hb {input.bam} {wildcards.chromosome} | samtools sort - {output}"
        

